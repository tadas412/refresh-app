<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>


<table>
	<tr>
		<td>user_name</td>
		<td>email</td>
		<td>num_reviews</td>
		<td>num_events</td>
		<td>avg_rating</td>
		<td>admin?</td>
	</tr>
	<% @users.each do |user| %>
	<tr>
		<td><%= user.user_name %></td>
		<td><%= user.email %></td>
		<td><%= user.num_reviews %></td>
		<td><%= user.num_events %></td>
		<td><%= user.avg_rating %></td>
		<td><%= user.admin %></td>
		<td><%= link_to "view", user_path(user.id) %></td>
		<% if @cur_user.admin and @cur_user.id != user.id %>
			<% if user.admin %>
				<td> (Remove Admin Link) </td>
			<% else %>
				<td> (Make Admin Link) </td>
			<% end %>
		<% end %>
	</tr>
	<% end %>
</table>